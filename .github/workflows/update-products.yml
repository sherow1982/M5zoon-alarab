name: 🚀 Generate Products Data from Source

on:
  # تشغيل يومياً الساعة 3 صباحاً UTC (5 صباحاً +02)
  schedule:
    - cron: '0 3 * * *'

  # التشغيل اليدوي
  workflow_dispatch:

jobs:
  generate:
    name: 🚀 Generate Data from Source
    runs-on: ubuntu-latest
    
    steps:
      # 1. نسخ الرمز
      - name: ⤵️ Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      # 2. تركيب Node.js
      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18.x'
          cache: 'npm'

      # 3. تنفيذ برنامج البيانات
      - name: 🚀 Generate Data from Source
        run: |
          export LANG=en_US.UTF-8
          export LC_ALL=en_US.UTF-8
          node generate-data-from-source.js

      # 4. التحقق من التغييرات
      - name: 🔍 Check for Changes
        id: verify
        run: |
          if git diff --quiet; then
            echo "No changes detected"
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            echo "Changes detected"
            echo "has_changes=true" >> $GITHUB_OUTPUT
            git diff --stat
          fi

      # 5. حفظ البيانات
      - name: ➡️ Commit & Push Changes
        if: steps.verify.outputs.has_changes == 'true'
        run: |
          git config --global user.email "action@github.com"
          git config --global user.name "GitHub Data Bot"
          git add -A
          git commit -m "🚀 AUTO: Generated products data & feeds from source - $(date)"
          git push origin main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 6. طباعة النتائج
      - name: ✅ Success Report
        run: |
          echo "════════════════════════════════════════"
          echo "✅ تم بنجاح - All Data Generated!"
          echo "════════════════════════════════════════"
          echo "📋 الملفات المنشأة:"
          echo "   ✓ بيانات فعلية من ملفات المنتجات"
          echo "   ✓ صور حقيقية من image_link"
          echo "   ✓ CSV feeds (عربي + انجليزي + بسيط)"
          echo "   ✓ XML feed"
          echo "   ✓ Schema markup"
          echo "════════════════════════════════════════"

      # 7. الالعان عن الأخطاء
      - name: ❌ Handle Errors
        if: failure()
        run: |
          echo "❌ فشلها العملية!"
          echo "راجع السجلات للتفاصيل"
          exit 1
