name: 🚀 Merge & Generate Products Data

on:
  schedule:
    - cron: '0 3 * * *'
  workflow_dispatch:

jobs:
  merge:
    name: 🚀 Merge Products & Generate Feeds & Pages
    runs-on: ubuntu-latest
    
    steps:
      - name: ⤵️ Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 🚀 Merge & Generate Data
        run: |
          export LANG=en_US.UTF-8
          export LC_ALL=en_US.UTF-8
          node merge-products.js 2>&1
        continue-on-error: true

      - name: 📄 Generate Product Pages with Schema
        run: |
          export LANG=en_US.UTF-8
          export LC_ALL=en_US.UTF-8
          node generate-product-pages.js 2>&1
        continue-on-error: true

      - name: 🔍 Check for Changes
        id: verify
        run: |
          if git diff --quiet; then
            echo "No changes detected"
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            echo "Changes detected"
            echo "has_changes=true" >> $GITHUB_OUTPUT
            git diff --stat
          fi

      - name: ➡️ Commit & Push Changes
        if: steps.verify.outputs.has_changes == 'true'
        run: |
          git config --global user.email "action@github.com"
          git config --global user.name "GitHub Data Bot"
          git add -A
          git commit -m "🚀 AUTO: Generated product pages with schema - $(date)"
          git push origin main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: ✅ Success
        run: |
          echo "════════════════════════════════════════"
          echo "✅ تم توليد كل البيانات!"
          echo "════════════════════════════════════════"
          echo "✓ منتجات مع بيانات كاملة"
          echo "✓ صفحات HTML لكل منتج"
          echo "✓ Schema محفور في كل صفحة"
          echo "✓ CSV + XML + JSON فيدات"

      - name: ❌ Handle Errors
        if: failure()
        run: |
          echo "❌ العملية انتهت لكن خذ بالك"
          exit 0
