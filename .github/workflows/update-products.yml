name: 🚀 Update Products Data & Schema

on:
  # تشغيل تلقائيب عند الرفع على main
  push:
    branches: [ main ]
    paths:
      - 'update-products.js'
      - 'package.json'

  # تشغيل يومي الساعة 3 صباحاً (UTC)
  schedule:
    - cron: '0 3 * * *'

  # التشغيل اليدويربوي من GitHub UI
  workflow_dispatch:

jobs:
  update:
    name: 📋 Update Products & Schema
    runs-on: ubuntu-latest
    
    steps:
      # 1. نسخ الرمز
      - name: ⤵️ Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      # 2. تركيب Node.js
      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18.x'
          cache: 'npm'

      # 3. تثبيت المتعلقات
      - name: 📑 Install Dependencies
        run: npm install

      # 4. تنفيذ Script التحديث
      - name: 🚀 Run Product Update
        run: node update-products.js

      # 5. التحقق مما إذا تغيرت الملفات
      - name: 🔍 Check for Changes
        id: verify
        run: |
          if git diff --quiet; then
            echo "No changes detected"
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            echo "Changes detected"
            echo "has_changes=true" >> $GITHUB_OUTPUT
            git diff --stat
          fi

      # 6. الرفع بالتغيرات
      - name: ➡️ Commit & Push Changes
        if: steps.verify.outputs.has_changes == 'true'
        run: |
          git config --global user.email "action@github.com"
          git config --global user.name "GitHub Action Bot"
          git add data/ public/schema/
          git commit -m "🚀 AUTO: Update products data & schema at $(date)"
          git push origin main

      # 7. طباعة النتائج
      - name: ✅ Success Report
        run: |
          echo "════════════════════════════════════════"
          echo "✅ Update Completed Successfully!"
          echo "════════════════════════════════════════"
          echo "📋 Files Updated:"
          echo "   • data/products.json"
          echo "   • data/perfumes.json"
          echo "   • data/watches.json"
          echo "   • public/schema/products-schema-ar.json"
          echo "   • public/schema/products-schema-en.json"
          echo "   • data/summary.json"
          echo ""

      # 8. الالعان عن الأخطاء
      - name: ❌ Handle Errors
        if: failure()
        run: |
          echo "❌ Update failed!"
          echo "Please check the logs above for details"
          exit 1

# الإخطارات:
# 1. The script runs automatically on push to main
# 2. The script runs daily at 3 AM UTC (5 AM +02 Dubai time)
# 3. You can also run it manually from GitHub Actions tab
# 4. All changes are automatically committed and pushed
# 5. The bot commits with timestamp
